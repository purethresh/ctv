  // TODO JLS
  // * Create User / Church selector
  // * Or allow Login
  // * After Login, or guest, go Back to previous page

export default function LoginPage() {
    // Option to login, or continue as guest
    // If Continue as guest, set cookie to guest and redirect to main page
    // After Login, set cookie to member and redirect to main page
  return (
    <div>Login page</div>
  );
}