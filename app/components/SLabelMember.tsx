import { Box } from "@mui/material";
// import { useEffect, useState } from 'react';

export default function SLabelMember() {
    // let [labelInfo, setLabelInfo] = useState<LabelInfo>(props.groupInfo || new LabelInfo({}));

    // TODO JLS


    return (
        <Box>
            Member goes here
        </Box>
    );



}