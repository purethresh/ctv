"use client"

// TODO JLS
//
// For a specific day, show all the schedulable labels
// Show the people linked to those labels linked to the schedule

export default function SServices(props:SServicesProp) {
  return (
    <div>Labels for Day</div>
  );
}